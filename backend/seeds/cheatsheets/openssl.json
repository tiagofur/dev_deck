{
  "slug": "openssl",
  "title": "OpenSSL Certificates & Crypto",
  "category": "security",
  "icon": "🔏",
  "color": "#721817",
  "description": "Generate certificates, inspect TLS connections, create CSRs, and debug SSL issues with OpenSSL.",
  "entries": [
    {
      "label": "Generate Self-Signed Cert",
      "command": "openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -sha256 -days 365 -nodes",
      "description": "Create a self-signed certificate for local development (no passphrase)",
      "tags": ["self-signed", "generate", "dev"]
    },
    {
      "label": "Generate CSR",
      "command": "openssl req -new -newkey rsa:4096 -nodes -keyout server.key -out server.csr",
      "description": "Create a Certificate Signing Request to submit to a CA",
      "tags": ["csr", "request", "ca"]
    },
    {
      "label": "View Certificate",
      "command": "openssl x509 -in cert.pem -text -noout",
      "description": "Display all certificate details — subject, issuer, validity, extensions",
      "tags": ["view", "inspect", "details"]
    },
    {
      "label": "Check Expiry",
      "command": "openssl x509 -enddate -noout -in cert.pem",
      "description": "Quick check when a certificate expires",
      "tags": ["expiry", "check", "validity"]
    },
    {
      "label": "Test Remote SSL",
      "command": "openssl s_client -connect example.com:443 -servername example.com",
      "description": "Connect to a remote server and inspect its TLS certificate chain",
      "tags": ["test", "remote", "tls"]
    },
    {
      "label": "Verify Certificate Chain",
      "command": "openssl verify -CAfile ca-bundle.crt cert.pem",
      "description": "Verify that a certificate is properly signed by a CA",
      "tags": ["verify", "chain", "ca"]
    },
    {
      "label": "Generate Random Password",
      "command": "openssl rand -base64 32",
      "description": "Generate a cryptographically secure random string",
      "tags": ["random", "password", "secret"]
    },
    {
      "label": "Hash a File (SHA256)",
      "command": "openssl dgst -sha256 file.tar.gz",
      "description": "Compute the SHA-256 hash of a file for integrity verification",
      "tags": ["hash", "sha256", "checksum"]
    }
  ]
}
